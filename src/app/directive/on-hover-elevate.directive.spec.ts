import { OnHoverElevateDirective } from './on-hover-elevate.directive';

describe('OnHoverElevateDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverElevateDirective();
    expect(directive).toBeTruthy();
  });
});
